# VAPID keys for web-push
    VAPID_PUBLIC=PUT_YOUR_PUBLIC_KEY_HERE
    VAPID_PRIVATE=PUT_YOUR_PRIVATE_KEY_HERE

    # SMTP settings
    SMTP_HOST=smtp.example.com
    SMTP_PORT=587
    SMTP_USER=username
    SMTP_PASS=password
    EMAIL_FROM="Clean Tracker" <no-reply@example.com>

    APP_URL=http://localhost:3000

    # Server port
    PORT=3000